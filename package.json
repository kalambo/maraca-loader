{
  "name": "maraca-loader",
  "version": "0.2.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && tsc",
    "prepare": "yarn run build"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "maraca": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.3"
  }
}
