{
  "name": "maraca-loader",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && tsc",
    "prepare": "yarn run build"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "fs-extra": "^7.0.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.4",
    "@types/node": "^10.12.12",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.2",
    "typescript": "^3.2.1"
  }
}
