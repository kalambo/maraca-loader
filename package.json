{
  "name": "maraca-loader",
  "version": "0.2.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && tsc",
    "prepare": "yarn run build"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "maraca": "*"
  },
  "devDependencies": {
    "prettier": "^1.18.2",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  }
}
