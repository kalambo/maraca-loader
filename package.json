{
  "name": "maraca-loader",
  "version": "0.2.4",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && tsc",
    "prepare": "yarn run build"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "maraca": "*"
  },
  "devDependencies": {
    "prettier": "^2.0.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
